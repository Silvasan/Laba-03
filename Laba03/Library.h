#pragma once
#include <stdio.h>
	int lenght_counter(int a, char arr[]);
	int delete_last_spaces(char* arr, char symbol, int lenght);
	int delete_lead_spaces(char* arr, char symbol);
	int delete_lead_and_last_spaces(char);
